services:
  api:
    build:
      context: api/
      dockerfile: Dockerfile
    container_name: fast-pin-pon-api
    env_file:
      - .env.example
    ports:
      - "8081:8080"
    environment:
      - LOG_FILE=/var/log/fast-pin-pon/api.log
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"
  
  front:
    build:
      context: front/
      dockerfile: Dockerfile
    container_name: fast-pin-pon-front
    ports:
      - "8080:80"
    networks:
      - internal
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"

  front:
    build:
      context: front/
      dockerfile: Dockerfile
    container_name: fast-pin-pon-front
    env_file:
      - .env.example
    ports:
      - "8080:80"
    networks:
      - internal
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"
    volumes:
      - ./front/nginx.conf:/etc/nginx/conf.d/default.conf

  postgres:
    image: ghcr.io/loan-mgt/fast-pin-pon/db:latest
    container_name: fast-pin-pon-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: fastpinpon
      POSTGRES_USER: fastpinpon
      POSTGRES_PASSWORD: fastpinpon
    volumes:
      - postgres-data:/var/lib/postgresql
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fastpinpon -d fastpinpon"]
      interval: 10s
      timeout: 5s
      retries: 5

  maptiler-server:
    image: maptiler/tileserver-gl:latest
    container_name: fast-pin-pon-map
    restart: unless-stopped
    ports:
      - "3650:8080"        # Exposes MapTiler Server API
    volumes:
      - ./maps:/data      # Mount your .mbtiles here
    networks:
      - internal
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"

networks:
  internal: null

volumes:
  postgres-data:
